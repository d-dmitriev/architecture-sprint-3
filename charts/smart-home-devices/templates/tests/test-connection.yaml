apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smart-home-devices.fullname" . }}-test-connection"
  labels:
    {{- include "smart-home-devices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smart-home-devices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
