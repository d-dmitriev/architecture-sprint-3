apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-home-devices.fullname" . }}
  labels:
    {{- include "smart-home-devices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart-home-devices.selectorLabels" . | nindent 4 }}
